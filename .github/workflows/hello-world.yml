name: Hello World Workflow

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Print Action 1
        run: echo "Hello, action 1 is GOOD!"

